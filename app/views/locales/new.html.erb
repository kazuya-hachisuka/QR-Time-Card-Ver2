<div class="form-group">
  <%= form_for @locale do |l|%>
    <%= l.label :ロケール名%>
    <%= l.text_field :locale_name, class: "form-control" %>
    <%= l.submit "作成", class: "btn btn-info"  %>
  <% end %>
</div>